(function () {
    'use strict';

    angular.module('mainApp').controller("priceController", priceController);

    priceController.$inject = ['$rootScope', '$scope', '$http'];

    function priceController($rootScope, $scope, $http) {
        var vm = this;
        vm.price = function () {
        }
    };
})();
